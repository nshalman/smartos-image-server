{
	"comments": {
		"listen_port": "port for node to listen on",
		"prefix": "prefix for urls of files being served. use https if behind SSL proxy",
		"suffix": "use ':<port>' if node or proxy is on nonstandard port",
		"loglevel": "use 'warn' to make the server quieter"
	},
	"listen_port": 80,
	"prefix": "http://",
	"suffix": "",
	"loglevel": "info"
}
